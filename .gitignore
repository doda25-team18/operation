# Environment files
.env
.env.local

# Docker Compose overrides
docker-compose.override.yml

# Docker volumes (if synced locally)
model_data/

inventory.cfg
.vagrant/

# K8s config file
kubeconfig

join_cmd.sh
