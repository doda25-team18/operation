apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-decision
  labels:
    grafana_dashboard: "1"
data:
  app-overview.json: |
{{ .Files.Get "dashboards/decision-dashboard.json" | indent 4 }}